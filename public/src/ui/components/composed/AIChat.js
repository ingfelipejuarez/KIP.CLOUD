/** KIP · AIChat — stub (la lógica real está en app/ai/ai.js) */
export const AIChatComponent = {
  init(habitos) {
    // La lógica completa del chat está en app/ai/ai.js (DeepSeekService).
    // Este stub evita errores de import en páginas que lo referencien.
    console.debug('[AIChatComponent] inicializado — chat gestionado por ai.js');
  },
};
